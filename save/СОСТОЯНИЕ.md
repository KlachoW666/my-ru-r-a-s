# Состояние на 2026-08-25

Ветка `main`, 18 коммитов, всё влито. Незакоммиченным остаётся только файл базы — он меняется от каждого запроса, это нормально.

## Проверенные цифры

| | |
|---|---|
| Сайт `localhost:3101` / админка `localhost:8080` | 200 / 200 |
| Каталог Steam | 5434 предмета |
| Кейсы | 5 шт, сломанных 0 |
| Обход Steam | идёт по кругу, 0 ошибок, 0 блокировок |
| Разделы админки | 65 проверок CRUD, 0 сбоев |
| Эндпоинты админки без обработчика | было 29 из 59, стало 0 |
| Ролевая модель админки | 36 проверок прав, 0 сбоев |
| Приватные баттлы | 17 проверок доступа, 0 сбоев |
| Регрессия после ролей | 58 разделов админки + 10 ручек сайта, 0 сбоев |

## Что было сделано

### Инфраструктура

- **Steam-авторизация** (OpenID 2.0) — [services/auth.js](../services/auth.js). JWT на `crypto`, refresh в httpOnly-cookie, allowlist для `redirect_to`.
- **Вход по e-mail** — [services/emailAuth.js](../services/emailAuth.js). Пароли `scrypt`, подтверждение, сброс. Ответы на «нет пользователя» и «неверный пароль» одинаковые, чтобы почту нельзя было перебирать.
- **Passkey-вход в админку** (WebAuthn) — [passkeys.js](../admin.titanrust.ru/server/passkeys.js). Первый ключ без приглашения, дальше `ADMIN_INVITE_CODE`. Ключ привязывается к строке в `admin_users` и берёт её роль.
- **Ролевая модель админки** — [adminAccess.js](../admin.titanrust.ru/server/adminAccess.js). Пять ролей, восемь доменов, проверка внутри `requireAdminJWT`.
- **Отправка писем** — [services/mailer.js](../services/mailer.js). Настоящий SMTP при заданном `SMTP_HOST`, иначе вывод в лог. Тракт проверен локальным приёмником: письмо дошло, код внутри тела.

### Каталог

- **5434 предмета** из Steam Market — [services/steamCatalog.js](../services/steamCatalog.js). Фоновый обход, возобновляемый после рестарта, круг ~27 минут.
- Редкость берётся из `name_color`, а не из цены. Цены конвертируются USD → ₽ по живому курсу.

### Игровая математика

- **Взвешенный розыгрыш** — [services/drops.js](../services/drops.js). Веса из `ticketRange` → `chance` → расчёта по RTP. Показанные шансы равны разыгрываемым.
- **Апгрейдер**: `chance = ставка / цель × RTP`. Проверено по 1500 игр на множитель.
- **Баттлы** — [services/battles.js](../services/battles.js). Состояние в SQLite, банк победителю. Приватные не показываются в лобби и открываются только по ссылке.
- **Розыгрыши** — [services/giveaways.js](../services/giveaways.js). Победитель честным броском, приз на баланс.
- **Инвентарь** — [services/inventory.js](../services/inventory.js). Предмет вместо денег, продажа, заявка на вывод скинов.
- **Provably fair с фиксацией до игры** — [services/fairness.js](../services/fairness.js). Публикуется только `sha256(serverSeed)`.
- **Защита от убыточных кейсов** — [services/caseHealth.js](../services/caseHealth.js). Открытие блокируется при отдаче выше `CASE_MAX_RTP`.

### Админка

- 20 недостающих таблиц, универсальная фабрика CRUD — [adminRoutes.js](../admin.titanrust.ru/server/adminRoutes.js), [adminSchema.js](../admin.titanrust.ru/server/adminSchema.js).
- Сводки по деньгам и RTP считаются из реальных транзакций.
- **Связка админка → сайт**: сайт читает те же таблицы. Выключенный в админке режим гаснет на сайте.
- **Раздел «Администраторы»**: заведение, смена роли, удаление вместе с ключами. Нельзя разжаловать себя и нельзя остаться без владельца.

## Чем подтверждено

Не «должно работать», а прогнано:

- **Provably fair** — раскрытый `serverSeed` дал тот самый хэш, что был опубликован до игры; бросок `nonce=0` пересчитался байт в байт (0.07877372).
- **RTP кейсов** — заявлено 95%, по 136 фактическим открытиям получилось 93.9%, частоты совпали с показанными шансами.
- **Апгрейдер** — x2: заявлено 47.50%, факт 45.73%; x4: 23.75% / 24.27%; x5: 19.00% / 21.13%.
- **Баттл** — 2 игрока, вход 1198 ₽ с каждого, банк 2396 ₽ ушёл победителю, балансы сошлись.
- **Инвентарь** — 5 кейсов дали 5 предметов, продажа вернула деньги, заявка заблокировала 2 из 4, отмена вернула все 4.
- **Промокоды** — фантомный код отбивается, лимит и повторное применение работают.
- **Баланс** — переживает перезапуск сервера (проверено на 7777 ₽).
- **Админка** — 17 разделов проходят создание → изменение → удаление, 8 кнопок-действий, 8 экранов настроек.
- **SMTP** — письмо принято локальным сервером с верными `From`/`To`/`Subject`, код 482913 найден в теле после декодирования MIME.
- **Права** — 36 проверок на живом сервере с `ADMIN_REQUIRE_AUTH=1` и настоящими токенами: модератор не подтверждает выводы и не правит RTP, финансист не блокирует игроков, наблюдатель не пишет никуда, мусорная роль в токене получает права наблюдателя, а не владельца. Catch-all тоже под защитой. Без токена — 401.
- **Раздел «Администраторы»** — 14 проверок: пароль не отдаётся, мусорная роль отбита, дубль логина отбит, себя не разжаловать и не удалить, последнего владельца не понизить, при двух владельцах понижение проходит.
- **Приватные баттлы** — 17 проверок: приватный замес не виден в лобби ни гостю, ни постороннему; создатель и вошедший игрок его видят; по ссылке открывается кем угодно, вход отрабатывает целиком до `finished`; uid приватного — 32 hex вместо 12.
- **Регрессия** — 58 разделов админки с выключенной проверкой отвечают как раньше, ни одного `catchAll`, ни одного 403. Владелец и администратор читают все 58, наблюдатель закрыт ровно на двух — это раздел «Администраторы». Десять ручек сайта живы.

## Исправленные баги, которых никто не искал

1. **Вечный спиннер на всех маршрутах.** `?v=2002` на entry-скрипте расщеплял ES-модуль на две копии, приложение монтировалось дважды, Vue уходил в бесконечный цикл в `removeFragment`.
2. **`attachAuth` регистрировался ниже игровых роутов** — `/cases/open`, апгрейдер, баттлы и инвентарь не видели токен и работали как гость.
3. **Промокод принимался любой** — `TESTCODE` давал +250 ₽.
4. **Цены занижены в ~83 раза** — Steam отдаёт USD, а код писал их как рубли.
5. **Редкость определялась по цене** — диапазоны цветов перекрываются почти полностью.
6. **13 связей `case_items`** вели на удалённые предметы; два кейса имели отдачу 3086%.
7. **Дедлок на 401** в `/user` — обработчик в бандле заканчивается промисом, который не резолвится никогда.
8. **4330 предметов ошибочно скрыты** — сравнение дат разных форматов.
9. **За отклонённое открытие списывались деньги** — проверка шла после списания.
10. **Любой passkey входил владельцем.** `register/verify` и `login/verify` выдавали токен «первого админа из таблицы», а не владельца ключа — роль в `admin_users` не значила ничего.
11. **Приватный баттл висел в общем лобби.** Флаг сохранялся, но список его не фильтровал: uid видел любой, вход ничем не отличался от публичного.


---

## Ребрендинг SATCHEL (сделан)

Игровой сайт переведён из TITAN RUST в SATCHEL / satchel.top. Режим B:
исходников Vue нет, бренд наложен слоем поверх собранного бандла.

Сделано: тема `satchel-theme.css`, знак и весь набор иконок, токен валюты,
20 артов кейсов, состояния баттлов, метаданные и SEO, 155 замен строк и 106
замен цвета в чанках, промо-баннер, документация.

Не сделано и почему: [DESIGN_MIGRATION_BLOCKERS.md](../DESIGN_MIGRATION_BLOCKERS.md).
Главное — нет hero-объекта, четырёх иллюстраций разделов и Spine-анимации
(каталога `public/spine/` в репозитории нет вовсе, анимация отдавала 404 и
до ребрендинга).

Админка не трогалась: это отдельная дизайн-система.
